{
  "name": "impossible-finance",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service masterbid/impossible-finance",
    "create-local": "graph create --node http://localhost:8020/ masterbid/impossible-finance",
    "remove-local": "graph remove --node http://localhost:8020/ masterbid/impossible-finance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 masterbid/impossible-finance"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
